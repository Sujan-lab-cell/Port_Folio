import { AchievementsClient } from "@/src/components/AchievementsClient";

export const metadata = {
  title: "Achievements & Credentials | Sujan K S - AI/ML Engineer",
  description: "Verified certificates, awards, course completions, and professional credentials of Sujan K S.",
};

export default function AchievementsPage() {
  return <AchievementsClient />;
}
